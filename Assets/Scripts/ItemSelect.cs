using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ItemSelect : MonoBehaviour
{
    //game over UI prefab reference
    private void OnMouseDown()
    {
        //throw up game over UI prefab
        //I could instead make it so the showing of win lose is in the game manager, then reference that?
    }
}
